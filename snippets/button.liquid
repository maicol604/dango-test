{% if variant=='primary' %}
    <a href="{{ link }}" class="bg-primary px-5 py-3 overflow-hidden rounded-lg text-xs flex items-center w-fit">
    {% else %}
    <a href="{{ link }}" class="b-primary px-5 py-3 overflow-hidden rounded-lg text-xs flex items-center w-fit">
{% endif %}
        <span>{{ text }}</span>
        <span>
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" d="M8.25 4.5l7.5 7.5-7.5 7.5" />
            </svg>   
        </span>       
    </a>